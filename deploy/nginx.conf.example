# /etc/nginx/sites-available/csk
# Замените example.ru на ваш домен. После: ln -s в sites-enabled, nginx -t, systemctl reload nginx
# SSL-сертификат проще всего выпустить через certbot (см. README).

server {
    listen 80;
    server_name example.ru www.example.ru;

    # Перенаправление на HTTPS (раскомментируйте после выпуска сертификата)
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# ---- HTTPS (после certbot) ----
# server {
#     listen 443 ssl http2;
#     server_name example.ru www.example.ru;
#
#     ssl_certificate     /etc/letsencrypt/live/example.ru/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/example.ru/privkey.pem;
#
#     gzip on;
#     gzip_types text/css application/javascript image/svg+xml application/json;
#
#     location / {
#         proxy_pass http://127.0.0.1:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Host              $host;
#         proxy_set_header X-Real-IP         $remote_addr;
#         proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
